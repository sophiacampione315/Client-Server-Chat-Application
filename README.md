# Client-Server-Chat-Application
-This is a simple Chat Application that allows multiple clients to connect to a server via UDP sockets. It uses arg parse when accepting commands, and sends data back and forth in the form of JSON strings.  
